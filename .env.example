# ── Neo4j ─────────────────────────────────────────────────────────────────
NEO4J_PASSWORD=flourisher123

# ── App credentials (single user) ─────────────────────────────────────────
APP_USERNAME=admin
APP_PASSWORD=changeme

# ── JWT ───────────────────────────────────────────────────────────────────
# Generate a strong secret: openssl rand -hex 32
JWT_SECRET=please-change-this-secret

# ── Ollama ─────────────────────────────────────────────────────────────────
# Ollama runs on the user's local machine — the frontend calls it directly.
# Configure the URL and model in the sidebar UI; they are stored in localStorage.
# Make sure Ollama is started with CORS enabled:
#   OLLAMA_ORIGINS=* ollama serve
