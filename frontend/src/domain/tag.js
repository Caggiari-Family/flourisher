/**
 * @typedef {{ id: string, name: string, description: string, suggested: boolean }} Tag
 * @typedef {{ id: string, source: string, target: string }} Edge
 * @typedef {{ nodes: Tag[], edges: Edge[] }} Graph
 */

export {};
